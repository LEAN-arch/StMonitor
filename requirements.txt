# ===================================================================
# Requirements for RedShield AI Command Suite - v4 (FINAL)
#
# Fixes all dependency conflicts:
# 1. Downgraded 'networkx' to 2.8.8 to be compatible with 'node2vec'.
# 2. Downgraded 'scipy' to 1.12.0 to resolve the 'triu' ImportError.
# 3. Pinned 'rich' to 13.7.1 to resolve the conflict with streamlit.
# ===================================================================

# -- Core Application & Data Handling --
streamlit==1.33.0
pandas==2.2.2
numpy==1.26.4

# -- Geospatial Libraries --
geopandas==0.14.4
shapely==2.0.4

# -- Network, ML & Scientific Computing --
networkx==2.8.8    # <-- CRITICAL FIX: Downgraded to be compatible with node2vec
node2vec==0.4.6
scikit-learn==1.4.2
torch==2.3.1
scipy==1.12.0      # <-- CRITICAL FIX: Downgraded for gensim compatibility

# -- Hidden Dependencies (Now Managed) --
gensim==4.3.2
rich==13.7.1       # <-- CRITICAL FIX: Pinned for streamlit compatibility

# -- Bayesian Modeling --
pymc==5.15.1

# -- Visualization --
altair==5.3.0
pydeck==0.9.0
matplotlib==3.8.3
