# ===================================================================
# Requirements for RedShield AI - vFINAL (Build 2)
# Solves all known dependency conflicts for web platform deployment.
# ===================================================================

# -- Core Framework --
streamlit==1.33.0

# -- Scientific Computing & ML --
# Pinning versions that are compatible with ALL other packages.
# networkx MUST be < 3.0 to be compatible with node2vec.
# This version is also compatible with a modern scipy.
networkx==2.8.8
scipy==1.12.0
numpy==1.26.4
scikit-learn==1.4.2

# -- Geospatial Libraries --
geopandas==0.14.4
shapely==2.0.4

# -- Graph Embeddings & Critical Dependencies --
# node2vec is old and has strict requirements.
node2vec==0.4.6
gensim==4.3.2
setuptools==69.5.1  # Provides the 'pkg_resources' module.

# -- Visualization & UI --
# Pinning rich to a version compatible with streamlit==1.33.0.
rich==13.7.1
altair==5.3.0
pydeck==0.9.0
matplotlib==3.8.3
